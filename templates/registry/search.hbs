<h2 class="centered">
  {{#if totalResults}}
    {{totalResults}}
  {{else}}
    Sorry, no
  {{/if}}
  result{{#unless singleResult}}s{{/unless}}
  for
  <a href="/search?q={{q}}">&lsquo;{{q}}&rsquo;</a>
</h2>

<ul class="columnar">
  {{#each results}}
    {{#with fields}}
      <li>{{> package-widget}}</li>
    {{/with}}
  {{/each}}
</ul>

{{> pagination}}
