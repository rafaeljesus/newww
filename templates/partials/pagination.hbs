{{#if pages}}
  <div class="pagination">
    {{#if pages.prev}}
      <a href="{{pages.prev}}" class="previous">Previous Page</a>
    {{/if}}

    {{#if pages.next}}
      <a href="{{pages.next}}" class="next">Next Page</a>
    {{/if}}
  </div>
{{/if}}
